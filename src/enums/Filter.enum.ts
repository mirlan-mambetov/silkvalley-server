export enum EnumProductPrice {
  HIGH_PRICE = 'high-price',
  LOW_PRICE = 'low-price',
}
export enum EnumProductSort {
  POPULAR = 'popular',
  VIEWS = 'views',
  OLDEST = 'oldest',
  NEWEST = 'newest',
}
